<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/login.css"> 
    <title>Könyvesbolt</title>
</head>
<body>
    <h1>Bejelentkezés</h1>
	<%  if (typeof(locals.errors) !== "undefined" && locals.errors.length !== 0) {	%>
		<div id="errors">
	<%	for (error of errors){ %> 
				<span class="error-message"><%- error -%></span><br/>
	<% 		} %>
		</div>	
	<% } %>
	<form class="form-container" action="/login" method="post">
		<label class="form-label" for="email">Email:</label>
		<input class="form-input" type="email" id="email" name="email" required>

		<label class="form-label" for="password">Jelszó:</label>
		<input class="form-input" type="password" id="password" name="password" required>

		<input class="form-submit" type="submit" value="Bejelentkezés">
	</form>

	<p>Nincs még fiókod? <button class="form-login" id="signupBtn" onclick="location.href='/register';">Regisztráció</button></p>
</body>
</html>