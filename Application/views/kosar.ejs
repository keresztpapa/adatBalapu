
 

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="stylesheets/style.css">
  <link rel="stylesheet" href="stylesheets/kosar.css">
  <!-- <style>
    table {
      margin: 20px auto;
      width: auto;
      border-collapse: collapse;
      max-height: 400px;
      overflow-y: auto;
    }
    
    th, td {
      border: 1px solid black;
      padding: 8px;
      text-align: center;
    }
    
    th {
      background-color: #eee;
    }
  </style>
  <style>
    table {
      width: auto;
      border-collapse: collapse;
      max-height: 400px;
      overflow-y: auto;
    }
    
    th, td {
      border: 1px solid black;
      padding: 8px;
      text-align: center;
    }
    
    th {
      background-color: #eee;
    }
  </style> -->
  <title>Könyvesbolt</title>
</head>
<body>
    <!-- Navbar -->
    <% include navbar %>
    
    
    <!-- Content area -->
    <div class="content">
      <h2> Kosár </h2>
      <table>
        <thead>
          <tr>
            <th>Email</th>
            <th>Cím</th>
            <th>Darabszam</th>
            <th>Hova</th>
          </tr>
        </thead>
        <tbody>
          <% data.forEach(function(row) { %>
            <tr>
              <td><%= row[0] %></td>
              <td><%= row[1] %></td>
              <td><%= row[2] %></td>
              <td><%= row[3] %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
    <!-- <table>
      <thead>
        <tr>
          <th>Email</th>
          <th>Cím</th>
          <th>Darabszam</th>
          <th>Hova</th>
        </tr>
      </thead>
      <tbody>
        <% data.forEach(function(row) { %>
          <tr>
            <td><%= row[0] %></td>
            <td><%= row[1] %></td>
            <td><%= row[2] %></td>
            <td><%= row[3] %></td>
          </tr>
        <% }); %>
      </tbody>
    </table> -->
  </body>
</html>